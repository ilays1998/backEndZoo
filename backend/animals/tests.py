from django.test import TestCase

# Create your tests here.
# test that there isn't a 2 same metadata identical
# test that there isn't a 2 animal with same name but differentw metadata
# test print with metadata == None
